---
- name: "Cleanup — tokens (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml
  tasks:
    - name: No-op — Alpha token already deleted by converge
      ansible.builtin.debug:
        msg: "Alpha token was deleted in converge — nothing to clean up"

...
