---
- name: "Cleanup — role_definitions (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml
  tasks:
    - name: No-op — int-roledef-alpha already deleted by converge
      ansible.builtin.debug:
        msg: "int-roledef-alpha was deleted in converge — nothing to clean up"

...
