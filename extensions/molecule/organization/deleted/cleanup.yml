---
- name: "Cleanup — organization (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: No-op — int-org-alpha already deleted by converge
    ansible.builtin.debug:
      msg: "int-org-alpha was deleted in converge — nothing to clean up"

