---
- name: "Cleanup — service_clusters (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml
  tasks:
    - name: No-op — int-cluster-alpha already deleted by converge
      ansible.builtin.debug:
        msg: "int-cluster-alpha was deleted in converge — nothing to clean up"

...
