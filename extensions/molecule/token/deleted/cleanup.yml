---
- name: "Cleanup — token (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: No-op — int-token-alpha already deleted by converge
    ansible.builtin.debug:
      msg: "int-token-alpha was deleted in converge — nothing to clean up"
