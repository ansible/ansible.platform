---
- name: "Cleanup — authenticators (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml
  tasks:
    - name: No-op — int-auth-alpha already deleted by converge
      ansible.builtin.debug:
        msg: "int-auth-alpha was deleted in converge — nothing to clean up"

...
