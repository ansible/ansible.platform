---
- name: "Verify — authenticators (overridden)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml
  tasks:
    - name: Gather all authenticators resources (verify overridden final state)
      ansible.platform.authenticators:
        state: gathered
        gateway_hostname: "{{ gateway_hostname }}"
        gateway_username: "{{ gateway_username }}"
        gateway_password: "{{ gateway_password }}"
        gateway_validate_certs: "{{ gateway_validate_certs }}"
      register: gathered_all

    - name: Assert int-auth-alpha still exists after overridden
      ansible.builtin.assert:
        that:
          - gathered_all.gathered is defined
          - gathered_all.gathered | selectattr("name", "equalto", "int-auth-alpha") | list | length == 1
        fail_msg: "Expected int-auth-alpha to exist after overridden. got: {{ gathered_all.gathered }}"

    - name: Assert int-auth-beta was deleted by overridden (not in desired config)
      ansible.builtin.assert:
        that:
          - gathered_all.gathered is defined
          - gathered_all.gathered | selectattr("name", "equalto", "int-auth-beta") | list | length == 0
        fail_msg: "overridden should have deleted int-auth-beta. got: {{ gathered_all.gathered }}"

...
