---
- name: "Verify — ui_plugin_route (overridden)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml
  tasks:
    - name: Gather target resource int-uipr-alpha (should exist after overridden)
      ansible.platform.ui_plugin_route:
        config:
          - name: "int-uipr-alpha"
        state: gathered
        gateway_hostname: "{{ gateway_hostname }}"
        gateway_username: "{{ gateway_username }}"
        gateway_password: "{{ gateway_password }}"
        gateway_validate_certs: "{{ gateway_validate_certs }}"
      register: gathered_target

    - name: Assert int-uipr-alpha still exists after overridden
      ansible.builtin.assert:
        that:
          - gathered_target.gathered is defined
          - gathered_target.gathered | length == 1
        fail_msg: "Expected int-uipr-alpha to exist after overridden. got: {{ gathered_target.gathered }}"

    - name: Gather extra resource int-uipr-beta (should be deleted by overridden)
      ansible.platform.ui_plugin_route:
        config:
          - name: "int-uipr-beta"
        state: gathered
        gateway_hostname: "{{ gateway_hostname }}"
        gateway_username: "{{ gateway_username }}"
        gateway_password: "{{ gateway_password }}"
        gateway_validate_certs: "{{ gateway_validate_certs }}"
      register: gathered_extra

    - name: Assert int-uipr-beta was deleted by overridden (not in desired config)
      ansible.builtin.assert:
        that:
          - gathered_extra.gathered is defined
          - gathered_extra.gathered | length == 0
        fail_msg: "overridden should have deleted int-uipr-beta. got: {{ gathered_extra.gathered }}"

...
