---
- name: "Verify — ui_plugin_routes (overridden)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml
  tasks:
    - name: Gather all ui_plugin_routes resources (verify overridden final state)
      ansible.platform.ui_plugin_routes:
        state: gathered
        gateway_hostname: "{{ gateway_hostname }}"
        gateway_username: "{{ gateway_username }}"
        gateway_password: "{{ gateway_password }}"
        gateway_validate_certs: "{{ gateway_validate_certs }}"
      register: gathered_all

    - name: Assert int-uipr-alpha still exists after overridden
      ansible.builtin.assert:
        that:
          - gathered_all.gathered is defined
          - gathered_all.gathered | selectattr("name", "equalto", "int-uipr-alpha") | list | length == 1
        fail_msg: "Expected int-uipr-alpha to exist after overridden. got: {{ gathered_all.gathered }}"

    - name: Assert int-uipr-beta was deleted by overridden (not in desired config)
      ansible.builtin.assert:
        that:
          - gathered_all.gathered is defined
          - gathered_all.gathered | selectattr("name", "equalto", "int-uipr-beta") | list | length == 0
        fail_msg: "overridden should have deleted int-uipr-beta. got: {{ gathered_all.gathered }}"

...
