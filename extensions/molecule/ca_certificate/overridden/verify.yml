---
- name: "Verify — ca_certificate (overridden)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml
  tasks:
    - name: Gather all ca_certificate resources (verify overridden final state)
      ansible.platform.ca_certificate:
        state: gathered
        gateway_hostname: "{{ gateway_hostname }}"
        gateway_username: "{{ gateway_username }}"
        gateway_password: "{{ gateway_password }}"
        gateway_validate_certs: "{{ gateway_validate_certs }}"
      register: gathered_all

    - name: Assert int-cert-alpha still exists after overridden
      ansible.builtin.assert:
        that:
          - gathered_all.gathered is defined
          - gathered_all.gathered | selectattr("name", "equalto", "int-cert-alpha") | list | length == 1
        fail_msg: "Expected int-cert-alpha to exist after overridden. got: {{ gathered_all.gathered }}"

    - name: Assert int-cert-beta was deleted by overridden (not in desired config)
      ansible.builtin.assert:
        that:
          - gathered_all.gathered is defined
          - gathered_all.gathered | selectattr("name", "equalto", "int-cert-beta") | list | length == 0
        fail_msg: "overridden should have deleted int-cert-beta. got: {{ gathered_all.gathered }}"

...
