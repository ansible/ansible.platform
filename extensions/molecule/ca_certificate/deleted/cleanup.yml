---
- name: "Cleanup — ca_certificate (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: No-op — int-cert-alpha already deleted by converge
    ansible.builtin.debug:
      msg: "int-cert-alpha was deleted in converge — nothing to clean up"
