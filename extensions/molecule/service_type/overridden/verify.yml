---
- name: "Verify — service_type (overridden)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml
  tasks:
    - name: Gather all service_type resources (verify overridden final state)
      ansible.platform.service_type:
        state: gathered
        gateway_hostname: "{{ gateway_hostname }}"
        gateway_username: "{{ gateway_username }}"
        gateway_password: "{{ gateway_password }}"
        gateway_validate_certs: "{{ gateway_validate_certs }}"
      register: gathered_all

    - name: Assert int-stype-alpha still exists after overridden
      ansible.builtin.assert:
        that:
          - gathered_all.gathered is defined
          - gathered_all.gathered | selectattr("name", "equalto", "int-stype-alpha") | list | length == 1
        fail_msg: "Expected int-stype-alpha to exist after overridden. got: {{ gathered_all.gathered }}"

    - name: Assert int-stype-beta was deleted by overridden (not in desired config)
      ansible.builtin.assert:
        that:
          - gathered_all.gathered is defined
          - gathered_all.gathered | selectattr("name", "equalto", "int-stype-beta") | list | length == 0
        fail_msg: "overridden should have deleted int-stype-beta. got: {{ gathered_all.gathered }}"

...
