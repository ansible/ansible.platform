---
- name: "Cleanup — routes (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml
  tasks:
    - name: No-op — int-route-alpha already deleted by converge
      ansible.builtin.debug:
        msg: "int-route-alpha was deleted in converge — nothing to clean up"

...
