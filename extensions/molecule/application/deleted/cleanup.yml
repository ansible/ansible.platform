---
- name: "Cleanup — application (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: No-op — int-app-alpha already deleted by converge
    ansible.builtin.debug:
      msg: "int-app-alpha was deleted in converge — nothing to clean up"
