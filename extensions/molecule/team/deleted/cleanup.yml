---
- name: "Cleanup — team (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: No-op — int-team-alpha already deleted by converge
    ansible.builtin.debug:
      msg: "int-team-alpha was deleted in converge — nothing to clean up"

