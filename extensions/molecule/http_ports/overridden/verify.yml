---
- name: "Verify — http_ports (overridden)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml
  tasks:
    - name: Gather all http_ports resources (verify overridden final state)
      ansible.platform.http_ports:
        state: gathered
        gateway_hostname: "{{ gateway_hostname }}"
        gateway_username: "{{ gateway_username }}"
        gateway_password: "{{ gateway_password }}"
        gateway_validate_certs: "{{ gateway_validate_certs }}"
      register: gathered_all

    - name: Assert int-port-alpha still exists after overridden
      ansible.builtin.assert:
        that:
          - gathered_all.gathered is defined
          - gathered_all.gathered | selectattr("name", "equalto", "int-port-alpha") | list | length == 1
        fail_msg: "Expected int-port-alpha to exist after overridden. got: {{ gathered_all.gathered }}"

    - name: Assert int-port-beta was deleted by overridden (not in desired config)
      ansible.builtin.assert:
        that:
          - gathered_all.gathered is defined
          - gathered_all.gathered | selectattr("name", "equalto", "int-port-beta") | list | length == 0
        fail_msg: "overridden should have deleted int-port-beta. got: {{ gathered_all.gathered }}"

...
