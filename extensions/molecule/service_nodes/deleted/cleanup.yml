---
- name: "Cleanup — service_nodes (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml
  tasks:
    - name: No-op — int-snode-alpha already deleted by converge
      ansible.builtin.debug:
        msg: "int-snode-alpha was deleted in converge — nothing to clean up"

...
