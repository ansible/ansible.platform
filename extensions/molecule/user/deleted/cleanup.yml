---
- name: "Cleanup — user (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: No-op — int-user-alice already deleted by converge
    ansible.builtin.debug:
      msg: "int-user-alice was deleted in converge — nothing to clean up"

