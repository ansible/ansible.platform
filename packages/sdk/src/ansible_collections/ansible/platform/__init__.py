"""Ansible Automation Platform Gateway SDK.

This package makes the ansible.platform collection importable via pip,
allowing non-Ansible consumers (MCP servers, CLIs, etc.) to use the
same SDK that powers the Ansible modules.
"""
