from __future__ import annotations
import time
from typing import Tuple
# from .state_client import ensure_manager, get_proxy  # integrate your demo-1 bus

def token_provider_from_bus() -> Tuple[str, float]:
    # proxy = get_proxy() ; token = proxy.get_token() ; expiry = proxy.get_expiry()
    # return token, expiry
    # For now, placeholder that you can wire to your manager.
    raise NotImplementedError
